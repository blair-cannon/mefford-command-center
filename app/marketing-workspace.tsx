"use client";

import { useEffect, useMemo, useState, type Dispatch, type ReactNode, type SetStateAction } from "react";
import Image from "next/image";
import { eligibleCompanyMembers } from "./company-directory";
import { CurrencyInput } from "./currency-input";
import { summaryDrilldownProps } from "./summary-drilldown";

const CAMPAIGN_TYPE = "Marketing Campaigns";
const CONTENT_TYPE = "Marketing Content";
const NEWSLETTER_TYPE = "Marketing Newsletter";
const ANALYTICS_TYPE = "Marketing Analytics Snapshot";
const CONFIG_TYPE = "Marketing Configuration";
const SURVEY_REQUEST_TYPE = "Marketing Customer Survey Request";
const SURVEY_RESPONSE_TYPE = "Marketing Customer Survey Response";

type MarketingActor = { name: string; email: string; accessLevel: "Company Owner" | "Administrator" | "Employee"; designations: string[] };
type MarketingRecord = { id: string; type: string; title: string; owner: string; due: string; status: string; meta: string; recordDate: string; createdAt?: string; updatedAt?: string; data: Record<string, unknown> };
type Asset = { id: number; name: string; contentType: string; size: number; uploadedBy: string; createdAt: string; url: string };
type Connection = { key: string; status: string; owner: string; lastCheckedAt: string; cause: string };
type AudienceContact = { id: string; name: string; company: string; email: string; status: "Eligible" | "Consent Review" | "Do Not Email" | "Suppressed"; source: string };
type SurveyProject = { number: string; name: string; status: string; ownerName: string; ownerEmail: string; contractType: string; contractStatus: string; startDate: string; substantialDate: string; finalDate: string; projectManager: string; superintendent: string; teamEmails: string[] };
type MarketingResponse = { permissions: { canView: boolean; canEdit: boolean; canApprove: boolean }; records: MarketingRecord[]; assets: Asset[]; surveyVideos: Asset[]; projects: SurveyProject[]; audienceContacts: AudienceContact[]; audiences: { externalContacts: number; totalExternalContacts: number; suppressedContacts: number; activeEmployees: number; externalRule: string; internalRule: string }; connections: Connection[]; controls: { sender: string; publishing: string; analytics: string; surveys: string }; error?: string };
type Tab = "Social Campaigns" | "Email Campaigns" | "Customer Surveys" | "Marketing Calendar" | "Connections";
type PostDraft = { id: string; title: string; campaignId: string; templateKey: string; contentPillar: string; format: string; platforms: string[]; body: string; callToAction: string; linkUrl: string; hashtags: string; imageAltText: string; publishAt: string; status: string; assetIds: number[]; notes: string };
type NewsletterDraft = { id: string; templateKey: string; emailType: string; audience: string; senderEmail: string; subject: string; previewText: string; body: string; ctaLabel: string; ctaUrl: string; scheduledAt: string; status: string; campaignId: string };

const socialPostTemplates = [
  { key: "project-spotlight", name: "Project Spotlight", pillar: "Project Story", format: "Photo", platforms: ["LinkedIn", "Facebook"], description: "Show the work, the team, and the customer outcome.", title: "Project Spotlight", body: "A closer look at the work taking shape at [PROJECT]. Our team is focused on [SCOPE / RESULT] while keeping safety, quality, and communication at the center of the job.", callToAction: "See more Mefford Contracting work", hashtags: "#MeffordContracting #Construction #DesignBuild" },
  { key: "milestone", name: "Project Milestone", pillar: "Progress", format: "Carousel", platforms: ["LinkedIn", "Facebook"], description: "Celebrate a signed contract, groundbreaking, topping-out, or completion.", title: "Project Milestone", body: "Milestone reached at [PROJECT]: [MILESTONE]. Thank you to [CUSTOMER / PARTNERS / TEAM] for helping move this work forward. Next up: [NEXT STEP].", callToAction: "Follow the project", hashtags: "#ProjectProgress #ConstructionManagement #MeffordContracting" },
  { key: "expert-tip", name: "Expert Tip", pillar: "Education", format: "Graphic", platforms: ["LinkedIn", "Facebook"], description: "Teach owners something useful before they begin a project.", title: "What Owners Should Know", body: "Before starting [PROJECT TYPE], owners should plan for [KEY CONSIDERATION]. The earlier this decision is made, the better the team can protect budget, schedule, and constructability.", callToAction: "Talk with our pre-construction team", hashtags: "#Preconstruction #ConstructionTips #DesignBuild" },
  { key: "team-culture", name: "Team & Culture", pillar: "People", format: "Photo", platforms: ["LinkedIn", "Facebook"], description: "Recognize an employee, hire, anniversary, safety win, or company moment.", title: "Meet The Team", body: "Today we are recognizing [EMPLOYEE / TEAM] for [CONTRIBUTION]. Their commitment to [VALUE] helps Mefford Contracting deliver better work for every customer.", callToAction: "Meet the people behind the work", hashtags: "#TeamMefford #ConstructionCareers #CompanyCulture" },
] as const;

const emailTemplates = [
  { key: "external-monthly", name: "External Monthly Update", audience: "External Contacts", emailType: "Newsletter", description: "Company news, project wins, useful insight, and one clear next step.", subject: "What’s New At Mefford Contracting", previewText: "Recent work, practical construction insight, and what is ahead.", body: "HELLO [FIRST NAME],\n\nPROJECT SPOTLIGHT\n[One concise project story and customer outcome.]\n\nWHAT OWNERS SHOULD KNOW\n[One useful planning, design, budget, or schedule insight.]\n\nAROUND MEFFORD\n[Company, team, or community update.]\n\nREADY TO TALK ABOUT A PROJECT?\n[Closing invitation and contact information.]", ctaLabel: "Start A Conversation" },
  { key: "project-announcement", name: "Project Announcement", audience: "External Contacts", emailType: "Announcement", description: "Announce an award, groundbreaking, opening, or completed project.", subject: "New Project: [PROJECT NAME]", previewText: "Mefford Contracting is proud to share our latest project.", body: "HELLO [FIRST NAME],\n\nWe are proud to announce [PROJECT / MILESTONE].\n\nTHE PROJECT\n[Customer, location, scope, and why the work matters.]\n\nTHE PLAN\n[Schedule, design/build approach, or next milestone.]\n\nThank you to [CUSTOMER / PARTNERS / TEAM].", ctaLabel: "View The Project" },
  { key: "employee-news", name: "Employee Company Update", audience: "Internal Employees", emailType: "Internal Update", description: "A clear internal newsletter for company priorities, people, safety, and dates.", subject: "Mefford Company Update — [MONTH]", previewText: "Priorities, project wins, people news, and important dates.", body: "TEAM,\n\nTHIS MONTH’S PRIORITIES\n[Three concise company priorities.]\n\nPROJECT & CUSTOMER WINS\n[Important wins and lessons.]\n\nSAFETY & QUALITY\n[Required focus or recognition.]\n\nPEOPLE & DATES\n[New hires, birthdays, anniversaries, events, and deadlines.]\n\nWHAT WE NEED FROM YOU\n[One clear action.]", ctaLabel: "Open The Company Calendar" },
  { key: "event-invitation", name: "Event Invitation", audience: "External Contacts", emailType: "Invitation", description: "Invite contacts to an event with the essential details and RSVP action.", subject: "You’re Invited: [EVENT NAME]", previewText: "Join Mefford Contracting on [DATE] at [LOCATION].", body: "HELLO [FIRST NAME],\n\nYou are invited to [EVENT NAME].\n\nDATE & TIME\n[DATE / TIME]\n\nLOCATION\n[LOCATION]\n\nWHAT TO EXPECT\n[Short event description.]\n\nPlease respond by [RSVP DATE].", ctaLabel: "RSVP" },
] as const;

const surveyProgramTemplates = [
  { milestone: "Design-Build", timing: "3 days after executed contract · 7 days after construction starts · weighted schedule at 50% · 7 days after owner turnover", focus: "Startup, design, construction, and completion", questions: "Five stars · Five questions · Comment · Photos" },
  { milestone: "Plan & Spec", timing: "Weighted schedule at 50% · 7 days after owner turnover", focus: "Construction midpoint and final delivery", questions: "Same midpoint and completion question sets" },
  { milestone: "Time & Materials", timing: "24 hours after each owner invoice · once per billing cycle", focus: "This month’s work and invoice clarity", questions: "Services · Clarity · Fairness · Hire Again" },
] as const;

const campaignStatuses = ["Planned", "Ready", "Active", "Paused", "Complete", "Cancelled"];
const channels = ["LinkedIn", "Facebook", "LinkedIn + Facebook"];
const money = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", minimumFractionDigits: 2, maximumFractionDigits: 2 });
function today() { return new Date().toLocaleDateString("en-CA", { timeZone: "America/New_York" }); }
function dateLabel(raw: string) { if (!raw) return "Not Set"; const date = new Date(`${raw.slice(0, 10)}T12:00:00`); return Number.isNaN(date.getTime()) ? raw : date.toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" }); }
function dateTimeLabel(raw: string) { if (!raw) return "Not Scheduled"; const date = new Date(raw); return Number.isNaN(date.getTime()) ? raw : date.toLocaleString("en-US", { month: "short", day: "numeric", year: "numeric", hour: "numeric", minute: "2-digit" }); }
function value(record: MarketingRecord, key: string) { return String(record.data[key] || ""); }
function numberValue(record: MarketingRecord, key: string) { return Number(record.data[key] || 0); }
function makeId(prefix: string) { return `${prefix}-${new Date().getFullYear()}-${crypto.randomUUID().slice(0, 8).toUpperCase()}`; }
function blankCampaign(actor: MarketingActor) { return { id: "", name: "", owner: actor.name, channel: "LinkedIn + Facebook", campaignType: "Brand Awareness", contentPillar: "Project Story", primaryKpi: "Qualified Leads", status: "Planned", audience: "", objective: "", callToAction: "", startDate: today(), endDate: "", budget: "", leadGoal: "", actualLeads: "", notes: "" }; }
function blankPost(): PostDraft { const template = socialPostTemplates[0]; return { id: "", title: template.title, campaignId: "", templateKey: template.key, contentPillar: template.pillar, format: template.format, platforms: [...template.platforms], body: template.body, callToAction: template.callToAction, linkUrl: "", hashtags: template.hashtags, imageAltText: "", publishAt: `${today()}T09:00`, status: "Draft", assetIds: [], notes: "" }; }
function blankNewsletter(): NewsletterDraft { const template = emailTemplates[0]; return { id: "", templateKey: template.key, emailType: template.emailType, audience: template.audience, senderEmail: "marketing@meffcon.com", subject: template.subject, previewText: template.previewText, body: template.body, ctaLabel: template.ctaLabel, ctaUrl: "", scheduledAt: `${today()}T09:00`, status: "Draft", campaignId: "" }; }
function blankAnalytics() { return { id: "", platform: "LinkedIn", relatedId: "", snapshotDate: today(), impressions: "", reach: "", clicks: "", engagements: "", sessions: "", conversions: "", source: "Manual Provider Report", notes: "" }; }
function blankSurvey() { return { id: "", requestId: "", projectId: "", milestone: "Contract Signed", respondentName: "", respondentEmail: "", responseDate: today(), overallRating: "5", communication: "5", quality: "5", schedule: "5", professionalism: "5", value: "5", recommendation: "5", comments: "", testimonial: "", marketingConsent: false, videoId: 0 }; }
function blankLegacySurvey() { return { projectName: "", projectNumber: "", projectType: "Plan & Spec", completionDate: today(), customerName: "", customerEmail: "", templateKey: "completion" }; }

export function MarketingWorkspace({ actor, onNavigate, initialTab = "Social Campaigns" }: { actor: MarketingActor; onNavigate?: (target: string) => void; initialTab?: Exclude<Tab, "Connections"> }) {
  const [data, setData] = useState<MarketingResponse | null>(null);
  const tab: Exclude<Tab, "Connections"> = initialTab;
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [notice, setNotice] = useState("");
  const [query, setQuery] = useState("");
  const [campaignView, setCampaignView] = useState<"Active" | "Future" | "All">("Active");
  const [campaignDraft, setCampaignDraft] = useState(() => blankCampaign(actor));
  const [postDraft, setPostDraft] = useState(blankPost);
  const [newsletterDraft, setNewsletterDraft] = useState(blankNewsletter);
  const [analyticsDraft, setAnalyticsDraft] = useState(blankAnalytics);
  const [surveyDraft, setSurveyDraft] = useState(blankSurvey);
  const [legacySurvey, setLegacySurvey] = useState(blankLegacySurvey);
  const [modal, setModal] = useState<"campaign" | "post" | "newsletter" | "analytics" | "survey" | "legacy-survey" | "config" | "">("");
  const [uploading, setUploading] = useState(false);
  const [calendarMonth, setCalendarMonth] = useState(() => new Date(`${today().slice(0, 7)}-01T12:00:00`));
  const [configDraft, setConfigDraft] = useState({ senderEmail: "marketing@meffcon.com", websiteUrl: "https://meffcon.com", gaPropertyId: "", linkedInOrganization: "", facebookPage: "" });

  async function load() {
    const response = await fetch("/api/marketing");
    const result = await response.json() as MarketingResponse;
    if (!response.ok) throw new Error(result.error || "Marketing Command Is Unavailable");
    setData(result);
    const config = result.records.find((record) => record.type === CONFIG_TYPE);
    if (config) setConfigDraft({ senderEmail: value(config, "senderEmail") || "marketing@meffcon.com", websiteUrl: value(config, "websiteUrl") || "https://meffcon.com", gaPropertyId: value(config, "gaPropertyId"), linkedInOrganization: value(config, "linkedInOrganization"), facebookPage: value(config, "facebookPage") });
  }
  useEffect(() => { let cancelled = false; const timer = window.setTimeout(() => { void load().catch((error) => !cancelled && setNotice(error instanceof Error ? error.message : "Marketing Command Is Unavailable")).finally(() => !cancelled && setLoading(false)); }, 0); return () => { cancelled = true; window.clearTimeout(timer); }; }, []);

  const records = data?.records || [];
  const campaigns = records.filter((record) => record.type === CAMPAIGN_TYPE);
  const posts = records.filter((record) => record.type === CONTENT_TYPE);
  const newsletters = records.filter((record) => record.type === NEWSLETTER_TYPE);
  const analytics = records.filter((record) => record.type === ANALYTICS_TYPE);
  const surveyRequests = records.filter((record) => record.type === SURVEY_REQUEST_TYPE);
  const surveyResponses = records.filter((record) => record.type === SURVEY_RESPONSE_TYPE);
  const owners = useMemo(() => { const people = [...eligibleCompanyMembers("Marketing"), ...eligibleCompanyMembers("Sales Representative")].map((person) => ({ name: person.name, email: person.email })); const unique = new Map(people.map((person) => [person.email, person])); unique.set(actor.email, { name: actor.name, email: actor.email }); return [...unique.values()]; }, [actor.email, actor.name]);
  const socialCampaigns = campaigns.filter((record) => /linkedin|facebook/i.test(value(record, "channel")));
  const activeCampaigns = socialCampaigns.filter((record) => ["Ready", "Active", "Paused"].includes(record.status));
  const futureCampaigns = socialCampaigns.filter((record) => record.status === "Planned" || value(record, "startDate") > today());
  const shownCampaigns = (campaignView === "Active" ? activeCampaigns : campaignView === "Future" ? futureCampaigns : socialCampaigns).filter((record) => `${record.title} ${record.owner} ${record.meta} ${JSON.stringify(record.data)}`.toLowerCase().includes(query.toLowerCase()));
  const currentFuturePosts = posts.filter((record) => !["Cancelled", "Published"].includes(record.status) || value(record, "publishAt") >= new Date().toISOString().slice(0, 16));
  const analyticsTotals = analytics.reduce((total, record) => ({ impressions: total.impressions + numberValue(record, "impressions"), reach: total.reach + numberValue(record, "reach"), clicks: total.clicks + numberValue(record, "clicks"), engagements: total.engagements + numberValue(record, "engagements"), sessions: total.sessions + numberValue(record, "sessions"), conversions: total.conversions + numberValue(record, "conversions") }), { impressions: 0, reach: 0, clicks: 0, engagements: 0, sessions: 0, conversions: 0 });
  const emailAnalytics = analytics.filter((record) => value(record, "platform") === "Newsletter");
  const emailAnalyticsTotals = emailAnalytics.reduce((total, record) => ({ impressions: total.impressions + numberValue(record, "impressions"), reach: total.reach + numberValue(record, "reach"), clicks: total.clicks + numberValue(record, "clicks"), engagements: total.engagements + numberValue(record, "engagements"), sessions: total.sessions + numberValue(record, "sessions"), conversions: total.conversions + numberValue(record, "conversions") }), { impressions: 0, reach: 0, clicks: 0, engagements: 0, sessions: 0, conversions: 0 });
  const averageCustomerRating = surveyResponses.length ? surveyResponses.reduce((sum, record) => sum + numberValue(record, "overallRating"), 0) / surveyResponses.length : 0;
  const fiveStarResponses = surveyResponses.filter((record) => numberValue(record, "overallRating") >= 4.95);
  const customerResponseRate = surveyRequests.length ? Math.round(surveyResponses.length / surveyRequests.length * 100) : 0;
  const connection = (key: string) => data?.connections.find((item) => item.key === key);
  const healthy = (key: string) => connection(key)?.status === "Connected";
  const moduleScorecards = tab === "Social Campaigns"
    ? [{ label: "Active Campaigns", value: activeCampaigns.length, note: `${futureCampaigns.length} future plans` }, { label: "Posts In Production", value: currentFuturePosts.length, note: "Draft through scheduled" }, { label: "Post Engagements", value: analyticsTotals.engagements, note: "Verified snapshots" }, { label: "Website Sessions", value: analyticsTotals.sessions, note: "Recorded GA4 results" }]
    : tab === "Email Campaigns"
      ? [{ label: "External Audience", value: data?.audiences.externalContacts || 0, note: `${data?.audiences.suppressedContacts || 0} suppressed` }, { label: "Active Employees", value: data?.audiences.activeEmployees || 0, note: "Internal audience" }, { label: "Email Drafts", value: newsletters.filter((record) => !["Sent", "Cancelled"].includes(record.status)).length, note: "Awaiting release" }, { label: "Email Clicks", value: emailAnalyticsTotals.clicks, note: "Verified snapshots" }]
      : tab === "Customer Surveys"
        ? [{ label: "Star Review Average", value: averageCustomerRating ? `${averageCustomerRating.toFixed(1)} ★` : "—", note: "Every verified review" }, { label: "Total Reviews", value: surveyResponses.length, note: "All recorded responses" }, { label: "5-Star Reviews", value: fiveStarResponses.length, note: "Average of 4.95+" }, { label: "Response Rate", value: `${customerResponseRate}%`, note: `${surveyResponses.length} of ${surveyRequests.length} requests` }]
        : [{ label: "Social Dates", value: calendarItems(socialCampaigns, posts, [], []).length, note: "Campaigns and posts" }, { label: "Email Dates", value: calendarItems([], [], newsletters, []).length, note: "Planned deliveries" }, { label: "Survey Dates", value: calendarItems([], [], [], surveyRequests).length, note: "Customer milestones" }, { label: "Upcoming Actions", value: calendarQueue(socialCampaigns, posts, newsletters, surveyRequests).length, note: "Company marketing queue" }];

  async function saveRecord(type: string, record: Omit<MarketingRecord, "type" | "createdAt" | "updatedAt">, success: string) {
    setSaving(true); setNotice("");
    try { const response = await fetch("/api/marketing", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "save-record", recordType: type, record }) }); const result = await response.json() as { error?: string }; if (!response.ok) throw new Error(result.error || "The Marketing Record Could Not Be Saved"); await load(); setModal(""); setNotice(success); }
    catch (error) { setNotice(error instanceof Error ? error.message : "The Marketing Record Could Not Be Saved"); } finally { setSaving(false); }
  }
  async function runMarketingAction(action: "publish-social" | "send-newsletter" | "send-test-newsletter" | "sync-analytics", recordId: string, success: string) {
    setSaving(true); setNotice("");
    try {
      const response = await fetch("/api/marketing", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action, recordId }) });
      const result = await response.json() as { error?: string; message?: string; saved?: boolean };
      if (!response.ok) throw new Error(result.error || "The Marketing Action Could Not Be Completed");
      await load();
      setNotice(result.message || success);
    } catch (error) { setNotice(error instanceof Error ? error.message : "The Marketing Action Could Not Be Completed"); }
    finally { setSaving(false); }
  }
  async function setAudienceStatus(contactId: string, status: AudienceContact["status"]) {
    setSaving(true); setNotice("");
    try {
      const response = await fetch("/api/marketing", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "set-audience-status", contactId, status }) });
      const result = await response.json() as { error?: string };
      if (!response.ok) throw new Error(result.error || "The Audience Status Could Not Be Saved");
      await load();
      setNotice("Audience Eligibility And Suppression Status Saved Permanently.");
    } catch (error) { setNotice(error instanceof Error ? error.message : "The Audience Status Could Not Be Saved"); }
    finally { setSaving(false); }
  }
  function editCampaign(record?: MarketingRecord) { setCampaignDraft(record ? { id: record.id, name: record.title, owner: record.owner, channel: value(record, "channel") || "LinkedIn + Facebook", campaignType: value(record, "campaignType") || "Brand Awareness", contentPillar: value(record, "contentPillar") || "Project Story", primaryKpi: value(record, "primaryKpi") || "Qualified Leads", status: record.status, audience: value(record, "audience"), objective: value(record, "objective"), callToAction: value(record, "callToAction"), startDate: value(record, "startDate") || record.recordDate, endDate: value(record, "endDate"), budget: String(numberValue(record, "budget") || ""), leadGoal: String(numberValue(record, "leadGoal") || ""), actualLeads: String(numberValue(record, "actualLeads") || ""), notes: value(record, "notes") } : blankCampaign(actor)); setModal("campaign"); }
  function editPost(record?: MarketingRecord) { setPostDraft(record ? { id: record.id, title: record.title, campaignId: value(record, "campaignId"), templateKey: value(record, "templateKey") || "project-spotlight", contentPillar: value(record, "contentPillar") || "Project Story", format: value(record, "format") || "Photo", platforms: Array.isArray(record.data.platforms) ? record.data.platforms.map(String) : [], body: value(record, "body"), callToAction: value(record, "callToAction"), linkUrl: value(record, "linkUrl"), hashtags: value(record, "hashtags"), imageAltText: value(record, "imageAltText"), publishAt: value(record, "publishAt"), status: record.status, assetIds: Array.isArray(record.data.assetIds) ? record.data.assetIds.map(Number) : [], notes: value(record, "notes") } : blankPost()); setModal("post"); }
  function editNewsletter(record?: MarketingRecord) { setNewsletterDraft(record ? { id: record.id, templateKey: value(record, "templateKey") || "external-monthly", emailType: value(record, "emailType") || "Newsletter", audience: value(record, "audience"), senderEmail: value(record, "senderEmail"), subject: value(record, "subject"), previewText: value(record, "previewText"), body: value(record, "body"), ctaLabel: value(record, "ctaLabel"), ctaUrl: value(record, "ctaUrl"), scheduledAt: value(record, "scheduledAt"), status: record.status, campaignId: value(record, "campaignId") } : { ...blankNewsletter(), senderEmail: configDraft.senderEmail }); setModal("newsletter"); }
  function editSurvey(record?: MarketingRecord) { const projectId = record ? value(record, "projectId") : data?.projects[0]?.number || ""; const project = data?.projects.find((item) => item.number === projectId); const isRequest = record?.type === SURVEY_REQUEST_TYPE; setSurveyDraft(record ? { id: isRequest ? "" : record.id, requestId: isRequest ? record.id : value(record, "requestId"), projectId, milestone: value(record, "milestone") || "Contract Signed", respondentName: isRequest ? project?.ownerName || "" : value(record, "respondentName"), respondentEmail: isRequest ? project?.ownerEmail || value(record, "ownerEmail") : value(record, "respondentEmail"), responseDate: isRequest ? today() : record.recordDate || today(), overallRating: String(numberValue(record, "overallRating") || 5), communication: String(numberValue(record, "communication") || 5), quality: String(numberValue(record, "quality") || 5), schedule: String(numberValue(record, "schedule") || 5), professionalism: String(numberValue(record, "professionalism") || 5), value: String(numberValue(record, "value") || 5), recommendation: String(numberValue(record, "recommendation") || 5), comments: value(record, "comments"), testimonial: value(record, "testimonial"), marketingConsent: record.data.marketingConsent === true, videoId: numberValue(record, "videoId") } : { ...blankSurvey(), projectId }); setModal("survey"); }
  async function uploadAsset(file?: File) { if (!file) return; setUploading(true); setNotice(""); try { const form = new FormData(); form.set("action", "upload-asset"); form.set("file", file); const response = await fetch("/api/marketing", { method: "POST", body: form }); const result = await response.json() as { asset?: Asset; error?: string }; if (!response.ok || !result.asset) throw new Error(result.error || "The Image Could Not Be Uploaded"); await load(); setPostDraft((current) => ({ ...current, assetIds: [...new Set([...current.assetIds, result.asset!.id])] })); setNotice(`${result.asset.name} Added To The Permanent Marketing Asset Library.`); } catch (error) { setNotice(error instanceof Error ? error.message : "The Image Could Not Be Uploaded"); } finally { setUploading(false); } }
  async function uploadSurveyVideo(file?: File) { if (!file) return; setUploading(true); setNotice(""); try { const form = new FormData(); form.set("action", "upload-survey-video"); form.set("file", file); const response = await fetch("/api/marketing", { method: "POST", body: form }); const result = await response.json() as { surveyVideo?: Asset; error?: string }; if (!response.ok || !result.surveyVideo) throw new Error(result.error || "The Customer Video Could Not Be Uploaded"); await load(); setSurveyDraft((current) => ({ ...current, videoId: result.surveyVideo!.id })); setNotice(`${result.surveyVideo.name} Added To The Permanent Customer Voice Record.`); } catch (error) { setNotice(error instanceof Error ? error.message : "The Customer Video Could Not Be Uploaded"); } finally { setUploading(false); } }
  async function reconcileSurveys() { setSaving(true); setNotice(""); try { const response = await fetch("/api/marketing", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "reconcile-surveys" }) }); const result = await response.json() as { error?: string; result?: { created?: number; updated?: number; sent?: number; projects?: number } }; if (!response.ok) throw new Error(result.error || "Survey Milestones Could Not Be Reconciled"); await load(); setNotice(`${result.result?.projects || 0} Project(s) Checked · ${result.result?.created || 0} New Request(s) · ${result.result?.updated || 0} Existing Request(s) Refreshed · ${result.result?.sent || 0} Automatically Sent. Manual links remain available for every request.`); } catch (error) { setNotice(error instanceof Error ? error.message : "Survey Milestones Could Not Be Reconciled"); } finally { setSaving(false); } }
  async function createLegacySurvey() { setSaving(true); setNotice(""); try { const response = await fetch("/api/marketing", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "create-legacy-survey", legacy: legacySurvey }) }); const result = await response.json() as { error?: string; message?: string }; if (!response.ok) throw new Error(result.error || "The Historical Survey Could Not Be Created"); await load(); setModal(""); setLegacySurvey(blankLegacySurvey()); setNotice(result.message || "Historical Survey Created."); } catch (error) { setNotice(error instanceof Error ? error.message : "The Historical Survey Could Not Be Created"); } finally { setSaving(false); } }
  async function copySurveyLink(record: MarketingRecord) { const link = value(record, "surveyUrl"); if (!link) { setNotice("Run The Milestone Check To Create This Secure Survey Link."); return; } try { await navigator.clipboard.writeText(link); setNotice(`${record.title} Secure Link Copied.`); } catch { setNotice("The Link Could Not Be Copied Automatically. Open It And Copy It From The Browser Address Bar."); } }

  if (loading && !data) return <div className="marketing-loading">Preparing Marketing Command…</div>;
  return <div className="marketing-workspace marketing-command">
    {notice ? <div className={/could not|required|must|unavailable|connection/i.test(notice) ? "form-error" : "inline-success"}>{notice}<button onClick={() => setNotice("")}>×</button></div> : null}
    <MarketingModuleHeader tab={tab as Exclude<Tab, "Connections">} onSetup={() => setModal("config")} onCreate={() => tab === "Social Campaigns" ? editPost() : tab === "Email Campaigns" ? editNewsletter() : tab === "Customer Surveys" ? void reconcileSurveys() : onNavigate?.("Marketing Social")} />
    <section className={`marketing-scorecards marketing-scorecards-${tab.toLowerCase().replaceAll(" ", "-")}`}>{moduleScorecards.map((card) => <article key={card.label}><span>{card.label}</span><strong>{typeof card.value === "number" ? card.value.toLocaleString("en-US") : card.value}</strong><small>{card.note}</small></article>)}</section>

    {tab === "Social Campaigns" ? <SocialTemplateLibrary onUse={(template) => { setPostDraft({ ...blankPost(), templateKey: template.key, title: template.title, contentPillar: template.pillar, format: template.format, platforms: [...template.platforms], body: template.body, callToAction: template.callToAction, hashtags: template.hashtags, assetIds: [] }); setModal("post"); }} /> : null}
    {tab === "Social Campaigns" ? <section className="marketing-panel"><header><div><h2>Social Campaign Plans</h2></div><button className="primary-action" onClick={() => editCampaign()}>＋ New Campaign</button></header><div className="marketing-tools"><div>{(["Active", "Future", "All"] as const).map((item) => <button key={item} className={campaignView === item ? "active" : ""} onClick={() => setCampaignView(item)}>{item} <b>{item === "Active" ? activeCampaigns.length : item === "Future" ? futureCampaigns.length : campaigns.length}</b></button>)}</div><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search campaign, owner, or audience" /></div>{shownCampaigns.length ? <div className="marketing-campaign-list">{shownCampaigns.map((record) => <article key={record.id}><header><div><small>{record.id} · {value(record, "channel").toUpperCase()}</small><h3>{record.title}</h3><p>{value(record, "objective")}</p></div><i className={record.status.toLowerCase()}>{record.status}</i></header><dl><div><dt>Owner</dt><dd>{record.owner}</dd></div><div><dt>Campaign Type</dt><dd>{value(record, "campaignType") || "Brand Awareness"}</dd></div><div><dt>Run Dates</dt><dd>{dateLabel(value(record, "startDate"))} – {dateLabel(value(record, "endDate"))}</dd></div><div><dt>Primary KPI</dt><dd>{value(record, "primaryKpi") || "Qualified Leads"}</dd></div><div><dt>Lead Progress</dt><dd>{numberValue(record, "actualLeads").toLocaleString("en-US")} / {numberValue(record, "leadGoal").toLocaleString("en-US")}</dd></div></dl><footer><span>{value(record, "callToAction") || "Call To Action Not Set"}</span><button onClick={() => editCampaign(record)}>Review / Update</button></footer></article>)}</div> : <Empty title="No Campaigns In This View" action="Create Campaign" onClick={() => editCampaign()} />}</section> : null}
    {tab === "Social Campaigns" ? <MarketingCalendar month={calendarMonth} items={calendarItems(socialCampaigns, posts, [], [])} onPrevious={() => setCalendarMonth((current) => new Date(current.getFullYear(), current.getMonth() - 1, 1, 12))} onNext={() => setCalendarMonth((current) => new Date(current.getFullYear(), current.getMonth() + 1, 1, 12))} /> : null}
    {tab === "Social Campaigns" ? <div className="marketing-stack"><section className="marketing-panel"><header><div><h2>LinkedIn & Facebook</h2></div><button className="primary-action" onClick={() => editPost()}>＋ New Post</button></header><div className="marketing-connection-strip"><ConnectionPill label="LinkedIn" connection={connection("linkedin-company")} /><ConnectionPill label="Facebook" connection={connection("facebook-company")} /><span>{healthy("linkedin-company") || healthy("facebook-company") ? "Connected platforms require final approval before delivery." : "Drafting and approval are live. Direct publishing is connection-gated."}</span></div>{posts.length ? <div className="marketing-post-grid">{posts.map((record) => { const assetIds = Array.isArray(record.data.assetIds) ? record.data.assetIds.map(Number) : []; const asset = data?.assets.find((item) => assetIds.includes(item.id)); return <article key={record.id}>{asset ? <Image src={asset.url} alt="" width={640} height={360} unoptimized /> : <div className="marketing-post-no-image">IMAGE OPTIONAL</div>}<div><small>{(Array.isArray(record.data.platforms) ? record.data.platforms : []).join(" + ")} · {dateTimeLabel(value(record, "publishAt"))}</small><h3>{record.title}</h3><p>{value(record, "body")}</p><footer><i>{record.status}</i><button onClick={() => editPost(record)}>Open Studio</button></footer></div></article>; })}</div> : <Empty title="No Posts Drafted Yet" action="Create First Post" onClick={() => editPost()} />}</section><section className="marketing-analytics"><header><div><h2>Metrics That Drive Decisions</h2></div><button className="secondary-action" onClick={() => { setAnalyticsDraft(blankAnalytics()); setModal("analytics"); }}>＋ Record Snapshot</button></header><div className="marketing-analytics-grid"><Metric label="Impressions" metric={analyticsTotals.impressions} records={analytics} field="impressions" /><Metric label="Reach" metric={analyticsTotals.reach} records={analytics} field="reach" /><Metric label="Clicks" metric={analyticsTotals.clicks} records={analytics} field="clicks" /><Metric label="Engagements" metric={analyticsTotals.engagements} records={analytics} field="engagements" /><Metric label="Website Sessions" metric={analyticsTotals.sessions} records={analytics} field="sessions" /><Metric label="Conversions" metric={analyticsTotals.conversions} records={analytics} field="conversions" /></div><div className="marketing-analytics-table">{analytics.length ? analytics.map((record) => <article key={record.id}><span><b>{value(record, "platform")}</b><small>{dateLabel(record.recordDate)} · {value(record, "source")}</small></span><strong>{numberValue(record, "impressions").toLocaleString("en-US")} impressions</strong><strong>{numberValue(record, "clicks").toLocaleString("en-US")} clicks</strong><strong>{numberValue(record, "sessions").toLocaleString("en-US")} sessions</strong></article>) : <p>No analytics snapshots recorded. Connect the providers or enter a dated report without inventing data.</p>}</div></section></div> : null}
    {tab === "Email Campaigns" ? <EmailTemplateLibrary onUse={(template) => { setNewsletterDraft({ ...blankNewsletter(), templateKey: template.key, emailType: template.emailType, audience: template.audience, senderEmail: configDraft.senderEmail, subject: template.subject, previewText: template.previewText, body: template.body, ctaLabel: template.ctaLabel }); setModal("newsletter"); }} /> : null}
    {tab === "Email Campaigns" ? <section className="marketing-panel marketing-email-studio"><header><div><h2>Email Campaign Studio</h2></div><button className="primary-action" onClick={() => editNewsletter()}>＋ New Email Campaign</button></header><div className="marketing-audience-cards"><article><b>{data?.audiences.externalContacts || 0}</b><span>EXTERNAL CONTACTS</span><small>{data?.audiences.externalRule}</small></article><article><b>{data?.audiences.activeEmployees || 0}</b><span>ACTIVE EMPLOYEES</span><small>{data?.audiences.internalRule}</small></article><article><b>{configDraft.senderEmail}</b><span>DEDICATED SENDER</span><small>Mailbox connection, suppression review, test delivery, and final approval are required.</small></article></div>{newsletters.length ? <div className="marketing-newsletter-list">{newsletters.map((record) => <article key={record.id}><i>{value(record, "audience") === "Internal Employees" ? "IN" : "EX"}</i><span><small>{value(record, "emailType") || "Newsletter"} · {value(record, "audience")} · {dateTimeLabel(value(record, "scheduledAt"))}</small><strong>{value(record, "subject")}</strong><p>{value(record, "previewText") || value(record, "body").slice(0, 120)}</p></span><b>{record.status}</b><button onClick={() => editNewsletter(record)}>Open Composer</button></article>)}</div> : <Empty title="No Email Campaigns Yet" action="Create Email Campaign" onClick={() => editNewsletter()} />}</section> : null}
    {tab === "Social Campaigns" ? <MarketingDomainConnections title="Social Publishing Connections" keys={["linkedin-company", "facebook-company", "google-analytics"]} connections={data?.connections || []} onOpen={() => onNavigate?.("IT & Integrations")} /> : null}
    {tab === "Email Campaigns" ? <div className="marketing-stack"><MarketingCalendar month={calendarMonth} items={calendarItems([], [], newsletters, [])} onPrevious={() => setCalendarMonth((current) => new Date(current.getFullYear(), current.getMonth() - 1, 1, 12))} onNext={() => setCalendarMonth((current) => new Date(current.getFullYear(), current.getMonth() + 1, 1, 12))} /><EmailAnalyticsPanel records={emailAnalytics} totals={emailAnalyticsTotals} onAdd={() => { setAnalyticsDraft({ ...blankAnalytics(), platform: "Newsletter" }); setModal("analytics"); }} /><MarketingDomainConnections title="Email Delivery Connection" keys={["marketing-email"]} connections={data?.connections || []} onOpen={() => onNavigate?.("IT & Integrations")} /></div> : null}
    {tab === "Customer Surveys" ? <CustomerReviewDashboard responses={surveyResponses} requests={surveyRequests} /> : null}
    {tab === "Customer Surveys" ? <SurveyProgramTemplateLibrary /> : null}
    {tab === "Customer Surveys" ? <MarketingDomainConnections title="Survey Delivery Connections" keys={["marketing-email", "customer-survey-delivery"]} connections={data?.connections || []} onOpen={() => onNavigate?.("IT & Integrations")} /> : null}
    {tab === "Customer Surveys" ? <div className="marketing-stack"><CustomerVoicePanel requests={surveyRequests} responses={surveyResponses} projects={data?.projects || []} videos={data?.surveyVideos || []} controls={data?.controls.surveys || ""} averageRating={averageCustomerRating} saving={saving} onReconcile={reconcileSurveys} onLegacy={() => { setLegacySurvey(blankLegacySurvey()); setModal("legacy-survey"); }} onCapture={(request) => editSurvey(request)} onEdit={editSurvey} /><SurveyLinkControl requests={surveyRequests} onCopy={copySurveyLink} /></div> : null}

    {tab === "Social Campaigns" ? <SocialDeliveryQueue posts={posts} canApprove={Boolean(data?.permissions.canApprove)} saving={saving} onEdit={editPost} onPublish={(record) => void runMarketingAction("publish-social", record.id, `${record.title} Was Released To The Connected Social Publisher.`)} onSync={() => void runMarketingAction("sync-analytics", "", "Connected Social And Website Analytics Were Refreshed.")} /> : null}
    {tab === "Email Campaigns" ? <div className="marketing-stack"><AudienceControl contacts={data?.audienceContacts || []} saving={saving} onStatus={setAudienceStatus} /><EmailDeliveryQueue newsletters={newsletters} canApprove={Boolean(data?.permissions.canApprove)} saving={saving} onEdit={editNewsletter} onTest={(record) => void runMarketingAction("send-test-newsletter", record.id, `A Test Of ${record.title} Was Sent To Your Company Email.`)} onSend={(record) => void runMarketingAction("send-newsletter", record.id, `${record.title} Was Released To The Connected Marketing Mailbox.`)} /></div> : null}
    {tab === "Marketing Calendar" ? <div className="marketing-stack"><MarketingCalendar month={calendarMonth} items={calendarItems(socialCampaigns, posts, newsletters, surveyRequests)} onPrevious={() => setCalendarMonth((current) => new Date(current.getFullYear(), current.getMonth() - 1, 1, 12))} onNext={() => setCalendarMonth((current) => new Date(current.getFullYear(), current.getMonth() + 1, 1, 12))} /><MarketingScheduleQueue items={calendarQueue(socialCampaigns, posts, newsletters, surveyRequests)} onOpen={(type) => onNavigate?.(type === "Newsletter" ? "Marketing Email" : type === "Survey" ? "Marketing Surveys" : "Marketing Social")} /></div> : null}

    {modal ? <MarketingModal title={modalTitle(modal)} subtitle={modalSubtitle(modal)} onClose={() => setModal("")}>
      {modal === "campaign" ? <CampaignFormV2 draft={campaignDraft} setDraft={setCampaignDraft} owners={owners} saving={saving} onSave={() => void saveRecord(CAMPAIGN_TYPE, { id: campaignDraft.id || makeId("CAMPAIGN"), title: campaignDraft.name.trim(), owner: campaignDraft.owner, due: campaignDraft.endDate, status: campaignDraft.status, meta: `${campaignDraft.channel} · ${campaignDraft.campaignType} · ${campaignDraft.audience} · ${money.format(Number(campaignDraft.budget || 0))}`, recordDate: campaignDraft.startDate, data: { channel: campaignDraft.channel, campaignType: campaignDraft.campaignType, contentPillar: campaignDraft.contentPillar, primaryKpi: campaignDraft.primaryKpi, audience: campaignDraft.audience.trim(), objective: campaignDraft.objective.trim(), callToAction: campaignDraft.callToAction.trim(), startDate: campaignDraft.startDate, endDate: campaignDraft.endDate, budget: Number(campaignDraft.budget || 0), leadGoal: Number(campaignDraft.leadGoal || 0), actualLeads: Number(campaignDraft.actualLeads || 0), notes: campaignDraft.notes.trim() } }, `${campaignDraft.name} Saved To The Permanent Campaign Register.`)} /> : null}
      {modal === "post" ? <PostFormV2 draft={postDraft} setDraft={setPostDraft} campaigns={socialCampaigns} assets={data?.assets || []} uploading={uploading} saving={saving} onUpload={uploadAsset} onSave={() => void saveRecord(CONTENT_TYPE, { id: postDraft.id || makeId("POST"), title: postDraft.title.trim(), owner: actor.name, due: postDraft.publishAt, status: postDraft.status, meta: `${postDraft.platforms.join(" + ")} · ${postDraft.contentPillar} · ${dateTimeLabel(postDraft.publishAt)}`, recordDate: postDraft.publishAt.slice(0, 10), data: { ...postDraft, id: undefined } }, `${postDraft.title} Saved In Social Content Studio.`)} /> : null}
      {modal === "newsletter" ? <NewsletterFormV2 draft={newsletterDraft} setDraft={setNewsletterDraft} campaigns={campaigns} audiences={data?.audiences} saving={saving} connected={healthy("marketing-email")} onSave={() => void saveRecord(NEWSLETTER_TYPE, { id: newsletterDraft.id || makeId("NEWS"), title: newsletterDraft.subject.trim(), owner: actor.name, due: newsletterDraft.scheduledAt, status: newsletterDraft.status, meta: `${newsletterDraft.emailType} · ${newsletterDraft.audience} · From ${newsletterDraft.senderEmail}`, recordDate: newsletterDraft.scheduledAt.slice(0, 10), data: { ...newsletterDraft, id: undefined, recipientCountAtDraft: newsletterDraft.audience === "External Contacts" ? data?.audiences.externalContacts || 0 : data?.audiences.activeEmployees || 0 } }, `${newsletterDraft.subject} Saved In Email Campaign Studio.`)} /> : null}
      {modal === "analytics" ? <AnalyticsForm draft={analyticsDraft} setDraft={setAnalyticsDraft} related={[...posts, ...newsletters]} saving={saving} onSave={() => void saveRecord(ANALYTICS_TYPE, { id: analyticsDraft.id || makeId("ANALYTICS"), title: `${analyticsDraft.platform} · ${dateLabel(analyticsDraft.snapshotDate)}`, owner: actor.name, due: analyticsDraft.snapshotDate, status: "Recorded", meta: `${analyticsDraft.source} · ${Number(analyticsDraft.impressions || 0).toLocaleString("en-US")} Impressions`, recordDate: analyticsDraft.snapshotDate, data: { ...analyticsDraft, id: undefined, impressions: Number(analyticsDraft.impressions || 0), reach: Number(analyticsDraft.reach || 0), clicks: Number(analyticsDraft.clicks || 0), engagements: Number(analyticsDraft.engagements || 0), sessions: Number(analyticsDraft.sessions || 0), conversions: Number(analyticsDraft.conversions || 0) } }, `${analyticsDraft.platform} Snapshot Saved With Its Source And Date.`)} /> : null}
      {modal === "survey" ? <SurveyForm draft={surveyDraft} setDraft={setSurveyDraft} projects={data?.projects || []} requests={surveyRequests} videos={data?.surveyVideos || []} uploading={uploading} saving={saving} onUpload={uploadSurveyVideo} onSave={() => { const project = data?.projects.find((item) => item.number === surveyDraft.projectId); void saveRecord(SURVEY_RESPONSE_TYPE, { id: surveyDraft.id || makeId("SURVEY-RESPONSE"), title: `${project?.name || surveyDraft.projectId} · ${surveyDraft.milestone} Customer Response`, owner: actor.name, due: surveyDraft.responseDate, status: "Response Recorded", meta: `${Number(surveyDraft.overallRating).toFixed(1)}/5 · ${surveyDraft.respondentName} · ${surveyDraft.milestone}`, recordDate: surveyDraft.responseDate, data: { ...surveyDraft, id: undefined, overallRating: Number(surveyDraft.overallRating), communication: Number(surveyDraft.communication), quality: Number(surveyDraft.quality), schedule: Number(surveyDraft.schedule), professionalism: Number(surveyDraft.professionalism), value: Number(surveyDraft.value), recommendation: Number(surveyDraft.recommendation), displayConsent: surveyDraft.marketingConsent, projectName: project?.name || "", projectManager: project?.projectManager || "", superintendent: project?.superintendent || "", teamEmails: project?.teamEmails || [] } }, "Customer response, consent choice, project team links, and supporting video were saved permanently."); }} /> : null}
      {modal === "legacy-survey" ? <LegacySurveyForm draft={legacySurvey} setDraft={setLegacySurvey} saving={saving} onSave={() => void createLegacySurvey()} /> : null}
      {modal === "config" ? <ConfigForm draft={configDraft} setDraft={setConfigDraft} saving={saving} onSave={() => void saveRecord(CONFIG_TYPE, { id: "MARKETING-CONFIG", title: "Company Marketing Identity", owner: actor.name, due: today(), status: "Configured", meta: `${configDraft.senderEmail} · ${configDraft.websiteUrl}`, recordDate: today(), data: configDraft }, "Company Marketing Identity Saved. No Passwords Or Tokens Were Stored.")} /> : null}
    </MarketingModal> : null}
  </div>;
}

function MarketingCalendar({ month, items, onPrevious, onNext }: { month: Date; items: Array<{ id: string; date: string; type: string; title: string; status: string }>; onPrevious: () => void; onNext: () => void }) { const first = new Date(month.getFullYear(), month.getMonth(), 1, 12); const start = new Date(first); start.setDate(1 - first.getDay()); const days = Array.from({ length: 42 }, (_, index) => { const date = new Date(start); date.setDate(start.getDate() + index); return date; }); return <section className="marketing-panel marketing-calendar"><header><div><h2>Marketing Calendar</h2></div><nav><button onClick={onPrevious}>‹</button><strong>{month.toLocaleDateString("en-US", { month: "long", year: "numeric" })}</strong><button onClick={onNext}>›</button></nav></header><div className="marketing-calendar-weekdays">{["SUN", "MON", "TUE", "WED", "THU", "FRI", "SAT"].map((day) => <b key={day}>{day}</b>)}</div><div className="marketing-calendar-days">{days.map((day) => { const key = day.toLocaleDateString("en-CA"); const dateItems = items.filter((item) => item.date === key); return <article key={key} data-calendar-date={day.toLocaleDateString("en-US", { weekday: "short", month: "short", day: "numeric" })} className={day.getMonth() === month.getMonth() ? "" : "outside"}><span>{day.getDate()}</span>{dateItems.map((item) => <div key={item.id} className={item.type.toLowerCase()}><i>{item.type.slice(0, 2).toUpperCase()}</i><b>{item.title}</b><small>{item.status}</small></div>)}</article>; })}</div></section>; }
function calendarItems(campaigns: MarketingRecord[], posts: MarketingRecord[], newsletters: MarketingRecord[], surveys: MarketingRecord[]) { return [...campaigns.flatMap((record) => [{ id: `${record.id}-start`, date: value(record, "startDate"), type: "Campaign", title: `${record.title} Starts`, status: record.status }, { id: `${record.id}-end`, date: value(record, "endDate"), type: "Campaign", title: `${record.title} Ends`, status: record.status }]), ...posts.map((record) => ({ id: record.id, date: value(record, "publishAt").slice(0, 10), type: "Post", title: record.title, status: record.status })), ...newsletters.map((record) => ({ id: record.id, date: value(record, "scheduledAt").slice(0, 10), type: "Newsletter", title: record.title, status: record.status })), ...surveys.map((record) => ({ id: record.id, date: record.due.slice(0, 10), type: "Survey", title: record.title, status: record.status }))].filter((item) => /^\d{4}-\d{2}-\d{2}$/.test(item.date)); }
function calendarQueue(campaigns: MarketingRecord[], posts: MarketingRecord[], newsletters: MarketingRecord[], surveys: MarketingRecord[]) {
  return calendarItems(campaigns, posts, newsletters, surveys)
    .filter((item) => item.date >= today())
    .sort((left, right) => left.date.localeCompare(right.date))
    .slice(0, 18);
}

function SocialDeliveryQueue({ posts, canApprove, saving, onEdit, onPublish, onSync }: { posts: MarketingRecord[]; canApprove: boolean; saving: boolean; onEdit: (record: MarketingRecord) => void; onPublish: (record: MarketingRecord) => void; onSync: () => void }) {
  const ready = posts.filter((record) => !["Published", "Cancelled"].includes(record.status));
  return <section className="marketing-panel marketing-delivery-queue"><header><div><h2>Social Publishing Queue</h2></div><button className="secondary-action" disabled={saving} onClick={onSync}>↻ Sync Connected Analytics</button></header>{ready.length ? <div>{ready.map((record) => <article key={record.id}><span><small>{(Array.isArray(record.data.platforms) ? record.data.platforms : []).join(" + ")} · {dateTimeLabel(value(record, "publishAt"))}</small><strong>{record.title}</strong><p>{value(record, "body").slice(0, 180)}</p></span><i>{record.status}</i><div><button onClick={() => onEdit(record)}>Review</button>{canApprove ? <button className="marketing-delivery-action" disabled={saving} onClick={() => onPublish(record)}>Approve &amp; Publish</button> : <small>Owner/Admin Final Approval Required</small>}</div></article>)}</div> : <div className="marketing-empty compact"><strong>No Posts Waiting For Release</strong><span>New drafts and scheduled posts will appear here automatically.</span></div>}</section>;
}

function AudienceControl({ contacts, saving, onStatus }: { contacts: AudienceContact[]; saving: boolean; onStatus: (contactId: string, status: AudienceContact["status"]) => void }) {
  const [filter, setFilter] = useState("");
  const shown = contacts.filter((contact) => `${contact.name} ${contact.company} ${contact.email} ${contact.status}`.toLowerCase().includes(filter.toLowerCase()));
  return <section className="marketing-panel marketing-audience-control"><header><div><h2>External Newsletter Audience</h2></div><input value={filter} onChange={(event) => setFilter(event.target.value)} placeholder="Search name, company, email, or status" /></header>{shown.length ? <div>{shown.map((contact) => <article key={contact.id}><span><strong>{contact.name}</strong><small>{contact.company || "Company Not Set"} · {contact.email}</small></span><i className={contact.status.toLowerCase().replaceAll(" ", "-")}>{contact.status}</i><select aria-label={`Marketing email status for ${contact.name}`} value={contact.status} disabled={saving} onChange={(event) => onStatus(contact.id, event.target.value as AudienceContact["status"])}><option>Eligible</option><option>Consent Review</option><option>Do Not Email</option><option>Suppressed</option></select></article>)}</div> : <div className="marketing-empty compact"><strong>No Matching External Contacts</strong><span>Add contacts in Sales or change the search to manage the newsletter audience.</span></div>}</section>;
}

function EmailDeliveryQueue({ newsletters, canApprove, saving, onEdit, onTest, onSend }: { newsletters: MarketingRecord[]; canApprove: boolean; saving: boolean; onEdit: (record: MarketingRecord) => void; onTest: (record: MarketingRecord) => void; onSend: (record: MarketingRecord) => void }) {
  const ready = newsletters.filter((record) => !["Sent", "Cancelled"].includes(record.status));
  return <section className="marketing-panel marketing-delivery-queue"><header><div><h2>Email Release Queue</h2></div><b>{ready.length} Awaiting Release</b></header>{ready.length ? <div>{ready.map((record) => <article key={record.id}><span><small>{value(record, "audience")} · {dateTimeLabel(value(record, "scheduledAt"))}</small><strong>{value(record, "subject")}</strong><p>{value(record, "previewText") || value(record, "body").slice(0, 180)}</p></span><i>{record.status}</i><div><button onClick={() => onEdit(record)}>Review</button><button disabled={saving} onClick={() => onTest(record)}>Send Test</button>{canApprove ? <button className="marketing-delivery-action" disabled={saving} onClick={() => onSend(record)}>Approve &amp; Schedule</button> : <small>Owner/Admin Final Approval Required</small>}</div></article>)}</div> : <div className="marketing-empty compact"><strong>No Newsletters Waiting For Release</strong><span>New drafts and scheduled newsletters will appear here automatically.</span></div>}</section>;
}

function MarketingScheduleQueue({ items, onOpen }: { items: Array<{ id: string; date: string; type: string; title: string; status: string }>; onOpen: (type: string) => void }) {
  return <section className="marketing-panel marketing-schedule-queue"><header><div><h2>Upcoming Schedule</h2></div><b>{items.length} Upcoming</b></header>{items.length ? <div>{items.map((item) => <button key={item.id} onClick={() => onOpen(item.type)}><time>{dateLabel(item.date)}</time><i>{item.type}</i><span><strong>{item.title}</strong><small>{item.status}</small></span><b>Open →</b></button>)}</div> : <div className="marketing-empty compact"><strong>No Future Marketing Dates Yet</strong><span>Create a campaign, post, newsletter, or reached customer-survey milestone to populate this schedule.</span></div>}</section>;
}

function ConnectionPill({ label, connection }: { label: string; connection?: Connection }) { return <div className={`marketing-connection-pill ${connection?.status === "Connected" ? "connected" : "disconnected"}`}><i /><span><b>{label}</b><small>{connection?.status || "Not Configured"}</small></span></div>; }
function Metric({ label, metric, records, field }: { label: string; metric: number; records: MarketingRecord[]; field: string }) { return <article {...summaryDrilldownProps({ title: `${label} Marketing Evidence`, rows: records.filter((record) => numberValue(record, field) !== 0).map((record) => ({ id: record.id, title: record.title, subtitle: `${value(record, "platform")} · ${dateLabel(record.recordDate)}`, value: numberValue(record, field).toLocaleString("en-US"), status: record.status, meta: value(record, "source") })) })}><span>{label}</span><strong>{metric.toLocaleString("en-US")}</strong></article>; }
function Empty({ title, action, onClick }: { title: string; action: string; onClick: () => void }) { return <div className="marketing-empty"><strong>{title}</strong><button onClick={onClick}>{action}</button></div>; }
function MarketingModal({ title, subtitle, onClose, children }: { title: string; subtitle: string; onClose: () => void; children: ReactNode }) { return <div className="marketing-modal" role="presentation" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><section role="dialog" aria-modal="true"><header><div><h2>{title}</h2><span>{subtitle}</span></div><button aria-label="Close" onClick={onClose}>×</button></header>{children}</section></div>; }

function EmailAnalyticsPanel({ records, totals, onAdd }: { records: MarketingRecord[]; totals: { impressions: number; reach: number; clicks: number; engagements: number; sessions: number; conversions: number }; onAdd: () => void }) {
  return <section className="marketing-analytics"><header><div><h2>Delivery &amp; Engagement Evidence</h2></div><button className="secondary-action" onClick={onAdd}>＋ Record Email Results</button></header><div className="marketing-analytics-grid"><Metric label="Delivered / Reach" metric={totals.reach} records={records} field="reach" /><Metric label="Opens / Engagements" metric={totals.engagements} records={records} field="engagements" /><Metric label="Clicks" metric={totals.clicks} records={records} field="clicks" /><Metric label="Website Sessions" metric={totals.sessions} records={records} field="sessions" /><Metric label="Conversions" metric={totals.conversions} records={records} field="conversions" /><Metric label="Provider Impressions" metric={totals.impressions} records={records} field="impressions" /></div><div className="marketing-analytics-table">{records.length ? records.map((record) => <article key={record.id}><span><b>{value(record, "platform")}</b><small>{dateLabel(record.recordDate)} · {value(record, "source")}</small></span><strong>{numberValue(record, "reach").toLocaleString("en-US")} delivered</strong><strong>{numberValue(record, "engagements").toLocaleString("en-US")} opens</strong><strong>{numberValue(record, "clicks").toLocaleString("en-US")} clicks</strong></article>) : <p>No email result snapshots recorded. Drafting, audience review, and scheduling remain available before the mailbox connection.</p>}</div></section>;
}

function MarketingDomainConnections({ title, keys, connections, onOpen }: { title: string; keys: string[]; connections: Connection[]; onOpen: () => void }) {
  const selected = keys.map((key) => connections.find((item) => item.key === key) || { key, status: "Not Configured", owner: "Unassigned", lastCheckedAt: "", cause: "" });
  return <section className="marketing-domain-connections"><header><div><h2>{title}</h2></div><button className="secondary-action" onClick={onOpen}>Open IT &amp; Integrations</button></header><div>{selected.map((item) => <article key={item.key}><ConnectionPill label={connectionLabel(item.key)} connection={item} /><span>{connectionPurpose(item.key)}</span><small>{item.status === "Connected" ? `Last checked ${dateTimeLabel(item.lastCheckedAt)}` : item.cause || "Connection setup required"}</small></article>)}</div></section>;
}

function CustomerReviewDashboard({ responses, requests }: { responses: MarketingRecord[]; requests: MarketingRecord[] }) {
  const [index, setIndex] = useState(0);
  const ordered = [...responses].sort((left, right) => (right.updatedAt || right.recordDate).localeCompare(left.updatedAt || left.recordDate));
  const current = ordered[index % Math.max(1, ordered.length)];
  const rating = current ? numberValue(current, "overallRating") : 0;
  useEffect(() => { if (!ordered.length) return; const seconds = rating >= 4 ? 30 : 5; const timer = window.setTimeout(() => setIndex((value) => (value + 1) % ordered.length), seconds * 1000); return () => window.clearTimeout(timer); }, [ordered.length, rating]);
  const average = responses.length ? responses.reduce((sum, record) => sum + numberValue(record, "overallRating"), 0) / responses.length : 0;
  const fiveStars = responses.filter((record) => numberValue(record, "overallRating") >= 4.95).length;
  const responseRate = requests.length ? Math.min(100, Math.round(responses.length / requests.length * 100)) : 0;
  const consent = current?.data.displayConsent === true || current?.data.marketingConsent === true;
  const photoIds = consent && current && Array.isArray(current.data.photoIds) ? current.data.photoIds.map(Number).filter((id) => id > 0) : [];
  return <section className="marketing-review-dashboard"><header><div><h2>Every Review Counts. The Best Stay Longer.</h2></div><b>LIVE REVIEW ROTATION</b></header><div className="marketing-review-metrics"><article><span>STAR REVIEW AVERAGE</span><strong>{responses.length ? average.toFixed(1) : "—"} <i>★</i></strong></article><article><span>TOTAL REVIEWS</span><strong>{responses.length}</strong></article><article><span>5-STAR REVIEWS</span><strong>{fiveStars}</strong></article><article><span>RESPONSE RATE</span><strong>{responseRate}%</strong></article></div>{current ? <article className={`marketing-featured-review ${rating >= 4 ? "best" : "transparent"}`}><div><span className="marketing-review-stars">{"★".repeat(Math.max(1, Math.round(rating)))}</span><strong>{rating.toFixed(1)} / 5</strong><blockquote>{consent && value(current, "comments") ? `“${value(current, "comments")}”` : "Rating recorded. Identifying details and written feedback remain private by customer choice."}</blockquote><footer><span><b>{consent ? value(current, "respondentName") : "Anonymous Customer"}</b><small>{consent ? `${value(current, "projectName") || value(current, "projectId")} · ${value(current, "milestone")}` : `${value(current, "milestone")} · Details Kept Private`}</small></span><i>REVIEW {index % ordered.length + 1} OF {ordered.length} · {rating >= 4 ? 30 : 5} SECONDS</i></footer></div>{photoIds.length ? <aside>{photoIds.slice(0, 4).map((id) => <Image key={id} src={`/api/files?id=${id}`} alt="Customer project photo" width={360} height={220} unoptimized />)}</aside> : null}</article> : <div className="marketing-review-empty"><span>★★★★★</span><strong>Verified customer reviews will rotate here automatically.</strong></div>}</section>;
}

function LegacySurveyForm({ draft, setDraft, saving, onSave }: { draft: ReturnType<typeof blankLegacySurvey>; setDraft: Dispatch<SetStateAction<ReturnType<typeof blankLegacySurvey>>>; saving: boolean; onSave: () => void }) {
  const update = (key: keyof typeof draft, next: string) => setDraft((current) => ({ ...current, [key]: next }));
  const ready = Boolean(draft.projectName.trim() && draft.completionDate && draft.customerName.trim() && /^\S+@\S+\.\S+$/.test(draft.customerEmail));
  return <><div className="marketing-form-section"><h3>Create A One-Off Secure Survey</h3></div><div className="marketing-form-grid"><label className="wide">Historical Project Name<input value={draft.projectName} onChange={(event) => update("projectName", event.target.value)} /></label><label>Project Number <small>optional</small><input value={draft.projectNumber} onChange={(event) => update("projectNumber", event.target.value)} /></label><label>Project Type<select value={draft.projectType} onChange={(event) => update("projectType", event.target.value)}><option>Design-Build</option><option>Plan &amp; Spec</option><option>Time &amp; Materials</option></select></label><label>Completion Date<input type="date" value={draft.completionDate} onChange={(event) => update("completionDate", event.target.value)} /></label><label>Survey Question Set<select value={draft.templateKey} onChange={(event) => update("templateKey", event.target.value)}><option value="completion">Project Completion</option><option value="midpoint">Construction Midpoint</option><option value="monthly">Monthly T&amp;M</option><option value="startup">Project Startup</option><option value="design">Design Experience</option></select></label><label>Customer Name<input value={draft.customerName} onChange={(event) => update("customerName", event.target.value)} /></label><label>Customer Email<input type="email" value={draft.customerEmail} onChange={(event) => update("customerEmail", event.target.value)} /></label></div><ModalActions saving={saving} disabled={!ready} label={ready ? "Create & Send Historical Survey" : "Complete Historical Project And Customer"} onSave={onSave} /></>;
}

function SurveyLinkControl({ requests, onCopy }: { requests: MarketingRecord[]; onCopy: (record: MarketingRecord) => void }) {
  return <section className="marketing-panel survey-link-control"><header><div><h2>Secure Link Control</h2></div><b>{requests.filter((record) => Boolean(value(record, "surveyUrl"))).length} Ready</b></header>{requests.length ? <div className="survey-link-list">{requests.map((record) => { const link = value(record, "surveyUrl"); const responded = record.status === "Responded" || Boolean(value(record, "responseId")); return <article key={record.id}><div><small>{value(record, "projectId")} · {value(record, "milestone")}</small><strong>{record.title}</strong><span>{responded ? "Completed—link is closed" : link ? "Single-use link ready" : "Run milestone check to create link"}</span></div><i className={responded ? "complete" : link ? "ready" : "waiting"}>{responded ? "RESPONDED" : link ? "READY" : "WAITING"}</i>{!responded && link ? <div className="survey-link-actions"><button onClick={() => onCopy(record)}>Copy Link</button><a href={link} target="_blank" rel="noreferrer">Open &amp; Test ↗</a></div> : null}</article>; })}</div> : <div className="marketing-empty"><strong>No Survey Requests Yet</strong><span>Use Check Project Milestones to evaluate all projects and create every reached request.</span></div>}</section>;
}

function CustomerVoicePanel({ requests, responses, projects, videos, controls, averageRating, saving, onReconcile, onLegacy, onCapture, onEdit }: { requests: MarketingRecord[]; responses: MarketingRecord[]; projects: SurveyProject[]; videos: Asset[]; controls: string; averageRating: number; saving: boolean; onReconcile: () => void; onLegacy: () => void; onCapture: (record: MarketingRecord) => void; onEdit: (record: MarketingRecord) => void }) {
  const milestones = ["Project Startup", "Design Experience", "Construction Midpoint", "Project Completion", "Monthly T&M Check-In"];

  return <div className="customer-voice-stack"><section className="customer-voice-hero"><div><h2>Customer Voice</h2><span>{controls}</span></div><div><strong {...summaryDrilldownProps({ title: "Customer Survey Ratings", rows: responses.map((record) => ({ id: record.id, title: value(record, "respondentName") || record.title, subtitle: `${value(record, "projectId")} · ${value(record, "milestone")}`, value: `${numberValue(record, "overallRating").toFixed(1)} / 5`, status: record.status, onOpen: () => onEdit(record), openLabel: "Open Response →" })) })}>{averageRating ? averageRating.toFixed(1) : "—"}<small>AVERAGE / 5</small></strong><button disabled={saving} onClick={() => void onReconcile()}>{saving ? "Checking…" : "Check Automatic Triggers"}</button></div></section><section className="customer-milestones">{milestones.map((milestone, index) => { const matching = requests.filter((item) => value(item, "milestone") === milestone); const trigger = ["3 days after executed Design-Build contract", "7 days after construction starts", "Weighted schedule reaches 50%", "7 days after owner turnover", "24 hours after each T&M owner invoice"][index]; return <article key={milestone} {...summaryDrilldownProps({ title: `${milestone} Survey Requests`, rows: matching.map((record) => ({ id: record.id, title: record.title, subtitle: `${value(record, "projectId")} · Due ${dateLabel(record.due)}`, status: record.status, meta: value(record, "ownerEmail"), onOpen: () => { const response = responses.find((item) => value(item, "requestId") === record.id); if (response) onEdit(response); else onCapture(record); }, openLabel: "Open Survey →" })) })}><i>{index + 1}</i><div><b>{milestone}</b><span>{trigger}</span></div><strong>{matching.length}</strong></article>; })}</section><section className="marketing-panel"><header><div><h2>Customer Survey Queue</h2></div><div className="survey-queue-actions"><button className="secondary-action" onClick={onLegacy}>＋ Historical One-Off</button><button className="secondary-action" onClick={() => onCapture({ id: "", type: SURVEY_REQUEST_TYPE, title: "", owner: "", due: today(), status: "Manual", meta: "", recordDate: today(), data: { projectId: projects[0]?.number || "", milestone: "Project Completion" } })}>＋ Record Direct Feedback</button></div></header>{requests.length ? <div className="customer-survey-requests">{requests.map((record) => { const response = responses.find((item) => value(item, "requestId") === record.id); return <article key={record.id}><span className={`customer-survey-status status-${record.status.toLowerCase().replace(/[^a-z0-9]+/g, "-")}`}>{response ? "Responded" : record.status}</span><div><small>{value(record, "projectId")} · {value(record, "milestone")}</small><strong>{record.title}</strong><p>{value(record, "recipientEmail") || value(record, "ownerEmail") || "Primary email required"} · Due {dateLabel(record.due)}</p></div>{response ? <button onClick={() => onEdit(response)}>Open Response</button> : <button onClick={() => onCapture(record)}>Capture Response</button>}</article>; })}</div> : <Empty title="No Survey Triggers Reached Yet" action="Check Automatic Triggers" onClick={onReconcile} />}</section><section className="marketing-panel"><header><div><h2>Ratings, Comments & Photos</h2></div><b {...summaryDrilldownProps({ title: "Verified Customer Responses", rows: responses.map((record) => ({ id: record.id, title: value(record, "respondentName") || record.title, subtitle: `${value(record, "projectId")} · ${value(record, "milestone")}`, value: `${numberValue(record, "overallRating").toFixed(1)} / 5`, status: record.status, onOpen: () => onEdit(record), openLabel: "Review Response →" })) })}>{responses.length} Responses</b></header>{responses.length ? <div className="customer-response-grid">{responses.map((record) => { const video = videos.find((item) => item.id === numberValue(record, "videoId")); const photoIds = Array.isArray(record.data.photoIds) ? record.data.photoIds.map(Number).filter((id) => id > 0) : []; return <article key={record.id}><header><div><small>{value(record, "projectId")} · {value(record, "milestone")}</small><h3>{value(record, "respondentName")}</h3></div><strong>{numberValue(record, "overallRating").toFixed(1)}<small>/5</small></strong></header><p>{value(record, "comments") || "No written comment provided."}</p>{photoIds.length ? <div className="customer-response-photos">{photoIds.slice(0, 4).map((id) => <Image key={id} src={`/api/files?id=${id}`} alt="Customer project photo" width={220} height={140} unoptimized />)}</div> : null}<footer><span>{record.data.displayConsent === true || record.data.marketingConsent === true ? "DISPLAY CONSENT RECORDED" : "ANONYMOUS DISPLAY ONLY"}</span>{video ? <a href={video.url} target="_blank" rel="noreferrer">Open Video ↗</a> : null}<button onClick={() => onEdit(record)}>Review</button></footer></article>; })}</div> : <div className="marketing-empty"><strong>No Customer Responses Yet</strong><span>Responses will appear here automatically from secure surveys.</span></div>}</section></div>;
}

function SurveyForm({ draft, setDraft, projects, requests, videos, uploading, saving, onUpload, onSave }: { draft: ReturnType<typeof blankSurvey>; setDraft: Dispatch<SetStateAction<ReturnType<typeof blankSurvey>>>; projects: SurveyProject[]; requests: MarketingRecord[]; videos: Asset[]; uploading: boolean; saving: boolean; onUpload: (file?: File) => void; onSave: () => void }) {
  const update = (key: keyof typeof draft, next: string | boolean | number) => setDraft((current) => ({ ...current, [key]: next }));
  const selectedVideo = videos.find((item) => item.id === draft.videoId);
  const chooseRequest = (id: string) => { const request = requests.find((item) => item.id === id); const project = projects.find((item) => item.number === value(request || ({ data: {} } as MarketingRecord), "projectId")); setDraft((current) => ({ ...current, requestId: id, projectId: project?.number || current.projectId, milestone: request ? value(request, "milestone") : current.milestone, respondentName: current.respondentName || project?.ownerName || "", respondentEmail: current.respondentEmail || project?.ownerEmail || "" })); };
  return <><div className="marketing-form-grid"><label className="wide">Triggered Survey Request<select value={draft.requestId} onChange={(event) => chooseRequest(event.target.value)}><option value="">Verified Direct Feedback / No Automatic Request</option>{requests.map((record) => <option key={record.id} value={record.id}>{value(record, "projectId")} · {value(record, "milestone")} · {record.status}</option>)}</select></label><label>Project<select value={draft.projectId} onChange={(event) => update("projectId", event.target.value)}><option value="">Choose Project</option>{projects.map((project) => <option key={project.number} value={project.number}>{project.number} · {project.name}</option>)}</select></label><label>Survey Stage<select value={draft.milestone} onChange={(event) => update("milestone", event.target.value)}>{["Project Startup", "Design Experience", "Construction Midpoint", "Project Completion", "Monthly T&M Check-In"].map((item) => <option key={item}>{item}</option>)}</select></label><label>Customer Name<input value={draft.respondentName} onChange={(event) => update("respondentName", event.target.value)} /></label><label>Customer Email<input type="email" value={draft.respondentEmail} onChange={(event) => update("respondentEmail", event.target.value)} /></label><label>Response Date<input type="date" value={draft.responseDate} onChange={(event) => update("responseDate", event.target.value)} /></label>{(["overallRating", "communication", "quality", "schedule", "professionalism", "value", "recommendation"] as const).map((key) => <label key={key}>{key === "overallRating" ? "Overall Rating" : key === "recommendation" ? "Likelihood To Recommend" : key[0].toUpperCase() + key.slice(1)}<select value={draft[key]} onChange={(event) => update(key, event.target.value)}>{[5, 4, 3, 2, 1].map((rating) => <option key={rating} value={rating}>{"★".repeat(rating)} · {rating} / 5</option>)}</select></label>)}<label className="wide">Project Feedback<textarea rows={5} value={draft.comments} onChange={(event) => update("comments", event.target.value)} /></label><label className="wide">Additional Customer Words<textarea rows={4} value={draft.testimonial} onChange={(event) => update("testimonial", event.target.value)} /></label><label className="wide customer-consent"><input type="checkbox" checked={draft.marketingConsent} onChange={(event) => update("marketingConsent", event.target.checked)} /><span><b>Customer explicitly permitted display of identifying details and media</b></span></label></div><div className="customer-video-upload"><div><b>Optional Customer Video</b><span>{selectedVideo ? `${selectedVideo.name} selected` : "Any Video Format Up To 250 MB. The Original Remains Attached To The Response Evidence."}</span></div><label className="secondary-action"><input type="file" accept="video/mp4,video/webm,video/quicktime" disabled={uploading} onChange={(event) => { onUpload(event.target.files?.[0]); event.currentTarget.value = ""; }} />{uploading ? "Uploading…" : "＋ Upload Video"}</label></div><ModalActions saving={saving} label="Save Verified Customer Response" onSave={onSave} /></>;
}

function MarketingModuleHeader({ tab, onSetup, onCreate }: { tab: Exclude<Tab, "Connections">; onSetup: () => void; onCreate: () => void }) {
  const copy = {
    "Social Campaigns": { eyebrow: "SOCIAL MEDIA COMMAND", title: "Social Campaigns", description: "Plan campaigns, create platform-ready posts, manage approvals, publish, and measure LinkedIn and Facebook performance.", action: "＋ Create Social Post", steps: ["Plan", "Create", "Approve", "Publish", "Measure"] },
    "Email Campaigns": { eyebrow: "EMAIL MARKETING COMMAND", title: "Email Campaigns", description: "Build external and employee email campaigns with the correct audience, template, approvals, delivery controls, and results.", action: "＋ Create Email Campaign", steps: ["Audience", "Compose", "Test", "Approve", "Deliver"] },
    "Customer Surveys": { eyebrow: "CUSTOMER VOICE COMMAND", title: "Customer Surveys & Reviews", description: "Automate contract-type survey timing, secure delivery, reminders, photo-capable responses, and the transparent customer-review display.", action: "↻ Check Automatic Triggers", steps: ["Trigger", "Deliver", "Collect", "Display", "Improve"] },
    "Marketing Calendar": { eyebrow: "COMPANY MARKETING SCHEDULE", title: "Marketing Calendar", description: "Coordinate every campaign, post, email, and customer survey date without mixing the workspaces that create them.", action: "＋ Create Marketing Item", steps: ["See", "Prioritize", "Assign", "Open", "Complete"] },
  }[tab];
  return <section className={`marketing-hero marketing-module-hero marketing-module-${tab.toLowerCase().replaceAll(" ", "-")}`}><div><h1>{copy.title}</h1></div><div className="marketing-hero-actions"><button className="secondary-action" onClick={onSetup}>Marketing Setup</button><button className="primary-action large" onClick={onCreate}>{copy.action}</button></div></section>;
}

function SocialTemplateLibrary({ onUse }: { onUse: (template: (typeof socialPostTemplates)[number]) => void }) {
  return <section className="marketing-template-library marketing-social-templates"><header><div><h2>Social Post Templates</h2></div><b>{socialPostTemplates.length} Templates</b></header><div>{socialPostTemplates.map((template) => <article key={template.key}><i>{template.format}</i><h3>{template.name}</h3><p>{template.description}</p><dl><div><dt>Pillar</dt><dd>{template.pillar}</dd></div><div><dt>Built For</dt><dd>{template.platforms.join(" + ")}</dd></div></dl><button onClick={() => onUse(template)}>Use This Template</button></article>)}</div></section>;
}

function EmailTemplateLibrary({ onUse }: { onUse: (template: (typeof emailTemplates)[number]) => void }) {
  return <section className="marketing-template-library marketing-email-templates"><header><div><h2>Email Campaign Templates</h2></div><b>{emailTemplates.length} Templates</b></header><div>{emailTemplates.map((template) => <article key={template.key}><i>{template.emailType}</i><h3>{template.name}</h3><p>{template.description}</p><dl><div><dt>Audience</dt><dd>{template.audience}</dd></div><div><dt>CTA</dt><dd>{template.ctaLabel}</dd></div></dl><button onClick={() => onUse(template)}>Build From Template</button></article>)}</div></section>;
}

function SurveyProgramTemplateLibrary() {
  return <section className="marketing-template-library marketing-survey-templates"><header><div><h2>Contract-Type Survey Programs</h2></div><b>{surveyProgramTemplates.length} Programs</b></header><div>{surveyProgramTemplates.map((template, index) => <article key={template.milestone}><i>0{index + 1}</i><h3>{template.milestone}</h3><p>{template.focus}</p><dl><div><dt>Automatic Timing</dt><dd>{template.timing}</dd></div><div><dt>Question Set</dt><dd>{template.questions}</dd></div></dl><span className="marketing-template-status">PREPROGRAMMED & ACTIVE</span></article>)}</div></section>;
}

function CampaignFormV2({ draft, setDraft, owners, saving, onSave }: { draft: ReturnType<typeof blankCampaign>; setDraft: Dispatch<SetStateAction<ReturnType<typeof blankCampaign>>>; owners: Array<{ name: string; email: string }>; saving: boolean; onSave: () => void }) {
  const update = (key: keyof typeof draft, next: string) => setDraft((current) => ({ ...current, [key]: next }));
  const ready = Boolean(draft.name.trim() && draft.audience.trim() && draft.objective.trim() && draft.callToAction.trim() && draft.startDate && draft.endDate);
  return <><div className="marketing-form-section"><h3>Define The Campaign Before Creating Posts</h3></div><div className="marketing-form-grid"><label className="wide">Campaign Name<input value={draft.name} onChange={(event) => update("name", event.target.value)} /></label><label>Campaign Type<select value={draft.campaignType} onChange={(event) => update("campaignType", event.target.value)}>{["Brand Awareness", "Lead Generation", "Project Launch", "Recruiting", "Community", "Customer Education"].map((item) => <option key={item}>{item}</option>)}</select></label><label>Content Pillar<select value={draft.contentPillar} onChange={(event) => update("contentPillar", event.target.value)}>{["Project Story", "Progress", "Education", "People", "Community", "Recruiting"].map((item) => <option key={item}>{item}</option>)}</select></label><label>Primary KPI<select value={draft.primaryKpi} onChange={(event) => update("primaryKpi", event.target.value)}>{["Qualified Leads", "Website Visits", "Engagements", "Reach", "Applications", "Event Responses"].map((item) => <option key={item}>{item}</option>)}</select></label><label>Owner<select value={draft.owner} onChange={(event) => update("owner", event.target.value)}>{owners.map((person) => <option key={person.email}>{person.name}</option>)}</select></label><label>Channel<select value={draft.channel} onChange={(event) => update("channel", event.target.value)}>{channels.map((item) => <option key={item}>{item}</option>)}</select></label><label>Status<select value={draft.status} onChange={(event) => update("status", event.target.value)}>{campaignStatuses.map((item) => <option key={item}>{item}</option>)}</select></label><label>Start Date<input type="date" value={draft.startDate} onChange={(event) => update("startDate", event.target.value)} /></label><label>End Date<input type="date" value={draft.endDate} onChange={(event) => update("endDate", event.target.value)} /></label><label>Budget<CurrencyInput min="0" value={draft.budget} onValueChange={(next) => update("budget", next)} /></label><label>Lead Goal<input type="number" min="0" value={draft.leadGoal} onChange={(event) => update("leadGoal", event.target.value)} /></label><label>Actual Leads<input type="number" min="0" value={draft.actualLeads} onChange={(event) => update("actualLeads", event.target.value)} /></label><label className="wide">Target Audience<textarea rows={2} value={draft.audience} onChange={(event) => update("audience", event.target.value)} /></label><label className="wide">Campaign Objective<textarea rows={3} value={draft.objective} onChange={(event) => update("objective", event.target.value)} /></label><label className="wide">Required Call To Action<input value={draft.callToAction} onChange={(event) => update("callToAction", event.target.value)} /></label><label className="wide">Internal Notes<textarea rows={3} value={draft.notes} onChange={(event) => update("notes", event.target.value)} /></label></div><ModalActions saving={saving} disabled={!ready} label={ready ? "Save Permanent Campaign" : "Complete Required Strategy Fields"} onSave={onSave} /></>;
}

function PostFormV2({ draft, setDraft, campaigns, assets, uploading, saving, onUpload, onSave }: { draft: ReturnType<typeof blankPost>; setDraft: Dispatch<SetStateAction<ReturnType<typeof blankPost>>>; campaigns: MarketingRecord[]; assets: Asset[]; uploading: boolean; saving: boolean; onUpload: (file?: File) => void; onSave: () => void }) {
  const update = (key: keyof typeof draft, next: string | number[]) => setDraft((current) => ({ ...current, [key]: next }));
  const applyTemplate = (key: string) => { const template = socialPostTemplates.find((item) => item.key === key) || socialPostTemplates[0]; setDraft((current) => ({ ...current, templateKey: template.key, title: template.title, contentPillar: template.pillar, format: template.format, platforms: [...template.platforms], body: template.body, callToAction: template.callToAction, hashtags: template.hashtags })); };
  const requirements = [
    { label: "Working title", done: draft.title.trim().length >= 3 },
    { label: "Platform selected", done: draft.platforms.length > 0 },
    { label: "Post copy", done: draft.body.trim().length >= 25 },
    { label: "Call to action", done: draft.callToAction.trim().length >= 3 },
    { label: "Publish date", done: Boolean(draft.publishAt) },
    { label: "Alt text when pictures are used", done: !draft.assetIds.length || draft.imageAltText.trim().length >= 5 },
  ];
  const ready = requirements.every((item) => item.done);
  return <><section className="marketing-composer-layout"><div className="marketing-composer-main"><div className="marketing-form-section"><h3>Start With A Proven Post Structure</h3></div><div className="marketing-inline-template-picker">{socialPostTemplates.map((template) => <button key={template.key} className={draft.templateKey === template.key ? "active" : ""} onClick={() => applyTemplate(template.key)}><b>{template.name}</b><span>{template.pillar} · {template.format}</span></button>)}</div><div className="marketing-form-grid"><label className="wide">Working Title<input value={draft.title} onChange={(event) => update("title", event.target.value)} /></label><label>Campaign<select value={draft.campaignId} onChange={(event) => update("campaignId", event.target.value)}><option value="">Standalone Content</option>{campaigns.map((record) => <option key={record.id} value={record.id}>{record.title}</option>)}</select></label><label>Content Pillar<select value={draft.contentPillar} onChange={(event) => update("contentPillar", event.target.value)}>{["Project Story", "Progress", "Education", "People", "Community", "Recruiting"].map((item) => <option key={item}>{item}</option>)}</select></label><label>Post Format<select value={draft.format} onChange={(event) => update("format", event.target.value)}>{["Photo", "Carousel", "Video", "Graphic", "Text Only", "Link"].map((item) => <option key={item}>{item}</option>)}</select></label><label>Planned Publish<input type="datetime-local" value={draft.publishAt} onChange={(event) => update("publishAt", event.target.value)} /></label><fieldset className="wide marketing-platforms"><legend>Publish To</legend>{["LinkedIn", "Facebook"].map((platform) => <label key={platform}><input type="checkbox" checked={draft.platforms.includes(platform)} onChange={() => setDraft((current) => ({ ...current, platforms: current.platforms.includes(platform) ? current.platforms.filter((item) => item !== platform) : [...current.platforms, platform] }))} />{platform}</label>)}</fieldset><label className="wide">Post Copy<textarea rows={8} value={draft.body} onChange={(event) => update("body", event.target.value)} placeholder="Lead with the point, prove it with a real detail, and end with the next action." /></label><label className="wide">Required Call To Action<input value={draft.callToAction} onChange={(event) => update("callToAction", event.target.value)} /></label><label>Destination Link<input type="url" value={draft.linkUrl} onChange={(event) => update("linkUrl", event.target.value)} placeholder="https://" /></label><label>Hashtags<input value={draft.hashtags} onChange={(event) => update("hashtags", event.target.value)} /></label><label className="wide">Image Alt Text {draft.assetIds.length ? <small>Required because a picture is selected.</small> : <small>Required when pictures are added.</small>}<input value={draft.imageAltText} onChange={(event) => update("imageAltText", event.target.value)} placeholder="Describe the image for accessibility" /></label><label>Status<select value={draft.status} onChange={(event) => update("status", event.target.value)}><option>Draft</option><option>Ready For Approval</option><option>Approved — Connection Required</option><option>Cancelled</option></select></label><label>Internal Notes<input value={draft.notes} onChange={(event) => update("notes", event.target.value)} /></label></div></div><aside className="marketing-requirements"><h3>Ready To Save</h3><ul>{requirements.map((item) => <li key={item.label} className={item.done ? "done" : "missing"}><i>{item.done ? "✓" : "!"}</i>{item.label}</li>)}</ul><section><b>LinkedIn</b></section><section><b>Facebook</b></section></aside></section><div className="marketing-asset-picker"><header><div><strong>Pictures</strong><span>Upload Any Image Format And Select Every Picture That Belongs With This Post.</span></div><label className="secondary-action"><input type="file" accept="image/jpeg,image/png,image/webp,image/gif" disabled={uploading} onChange={(event) => { onUpload(event.target.files?.[0]); event.currentTarget.value = ""; }} />{uploading ? "Uploading…" : "＋ Upload Picture"}</label></header><div>{assets.length ? assets.map((asset) => <button key={asset.id} className={draft.assetIds.includes(asset.id) ? "selected" : ""} onClick={() => update("assetIds", draft.assetIds.includes(asset.id) ? draft.assetIds.filter((item) => item !== asset.id) : [...draft.assetIds, asset.id])}><Image src={asset.url} alt="" width={240} height={140} unoptimized /><span>{asset.name}</span><b>{draft.assetIds.includes(asset.id) ? "SELECTED" : "SELECT"}</b></button>) : <p>No pictures uploaded yet.</p>}</div></div><ModalActions saving={saving} disabled={!ready} label={ready ? "Save Social Post Draft" : "Complete Social Requirements"} onSave={onSave} /></>;
}

function NewsletterFormV2({ draft, setDraft, campaigns, audiences, saving, connected, onSave }: { draft: ReturnType<typeof blankNewsletter>; setDraft: Dispatch<SetStateAction<ReturnType<typeof blankNewsletter>>>; campaigns: MarketingRecord[]; audiences?: MarketingResponse["audiences"]; saving: boolean; connected: boolean; onSave: () => void }) {
  const update = (key: keyof typeof draft, next: string) => setDraft((current) => ({ ...current, [key]: next }));
  const applyTemplate = (key: string) => { const template = emailTemplates.find((item) => item.key === key) || emailTemplates[0]; setDraft((current) => ({ ...current, templateKey: template.key, emailType: template.emailType, audience: template.audience, subject: template.subject, previewText: template.previewText, body: template.body, ctaLabel: template.ctaLabel })); };
  const recipients = draft.audience === "External Contacts" ? audiences?.externalContacts || 0 : audiences?.activeEmployees || 0;
  const requirements = [
    { label: "Audience selected", done: ["External Contacts", "Internal Employees"].includes(draft.audience) },
    { label: "Subject line", done: draft.subject.trim().length >= 3 },
    { label: "Inbox preview text", done: draft.previewText.trim().length >= 10 },
    { label: "Complete message", done: draft.body.trim().length >= 40 },
    { label: "Call to action", done: draft.ctaLabel.trim().length >= 2 },
    { label: "Delivery date", done: Boolean(draft.scheduledAt) },
  ];
  const ready = requirements.every((item) => item.done);
  return <><section className="marketing-composer-layout marketing-email-composer"><div className="marketing-composer-main"><div className="marketing-recipient-preview"><strong>{recipients.toLocaleString("en-US")}</strong><span>Current {draft.audience} With Email · Audience Is Recalculated At Release</span></div><div className="marketing-form-section"><h3>Choose The Correct Message Structure</h3></div><div className="marketing-inline-template-picker">{emailTemplates.map((template) => <button key={template.key} className={draft.templateKey === template.key ? "active" : ""} onClick={() => applyTemplate(template.key)}><b>{template.name}</b><span>{template.audience} · {template.emailType}</span></button>)}</div><div className="marketing-form-grid"><label>Audience<select value={draft.audience} onChange={(event) => update("audience", event.target.value)}><option>External Contacts</option><option>Internal Employees</option></select></label><label>Email Type<select value={draft.emailType} onChange={(event) => update("emailType", event.target.value)}>{["Newsletter", "Announcement", "Internal Update", "Invitation"].map((item) => <option key={item}>{item}</option>)}</select></label><label>Dedicated Sender<input type="email" value={draft.senderEmail} onChange={(event) => update("senderEmail", event.target.value)} /></label><label>Campaign<select value={draft.campaignId} onChange={(event) => update("campaignId", event.target.value)}><option value="">Standalone Email</option>{campaigns.map((record) => <option key={record.id} value={record.id}>{record.title}</option>)}</select></label><label className="wide">Subject Line<input value={draft.subject} onChange={(event) => update("subject", event.target.value)} /></label><label className="wide">Inbox Preview Text<input value={draft.previewText} onChange={(event) => update("previewText", event.target.value)} /></label><label>Planned Delivery<input type="datetime-local" value={draft.scheduledAt} onChange={(event) => update("scheduledAt", event.target.value)} /></label><label>Status<select value={draft.status} onChange={(event) => update("status", event.target.value)}><option>Draft</option><option>Ready For Approval</option><option>Approved — Connection Required</option><option>Cancelled</option></select></label><label className="wide">Email Body<textarea rows={14} value={draft.body} onChange={(event) => update("body", event.target.value)} /></label><label>CTA Button Label<input value={draft.ctaLabel} onChange={(event) => update("ctaLabel", event.target.value)} /></label><label>CTA Destination<input type="url" value={draft.ctaUrl} onChange={(event) => update("ctaUrl", event.target.value)} placeholder="https://" /></label></div></div><aside className="marketing-requirements"><h3>Ready For Test</h3><ul>{requirements.map((item) => <li key={item.label} className={item.done ? "done" : "missing"}><i>{item.done ? "✓" : "!"}</i>{item.label}</li>)}</ul><section><b>External Email</b><span>Suppression, consent status, unsubscribe, sender identity, test delivery, and final approval.</span></section><section><b>Employee Email</b><span>Active employee audience, company sender, clear required action, and delivery evidence.</span></section></aside></section><div className="marketing-publish-boundary"><b>{connected ? "Mailbox Connected · Test And Approval Still Required" : "Marketing Mailbox Not Connected"}</b><span>Saving keeps the email inside Command Center. It cannot deliver until the release workflow is complete.</span></div><ModalActions saving={saving} disabled={!ready} label={ready ? "Save Email Campaign Draft" : "Complete Email Requirements"} onSave={onSave} /></>;
}

function AnalyticsForm({ draft, setDraft, related, saving, onSave }: { draft: ReturnType<typeof blankAnalytics>; setDraft: Dispatch<SetStateAction<ReturnType<typeof blankAnalytics>>>; related: MarketingRecord[]; saving: boolean; onSave: () => void }) { const update = (key: keyof typeof draft, next: string) => setDraft((current) => ({ ...current, [key]: next })); return <><div className="marketing-form-grid"><label>Source<select value={draft.platform} onChange={(event) => update("platform", event.target.value)}><option>LinkedIn</option><option>Facebook</option><option>Google Analytics</option><option>Newsletter</option></select></label><label>Snapshot Date<input type="date" value={draft.snapshotDate} onChange={(event) => update("snapshotDate", event.target.value)} /></label><label className="wide">Related Post / Newsletter<select value={draft.relatedId} onChange={(event) => update("relatedId", event.target.value)}><option value="">Website / Overall Channel</option>{related.map((record) => <option key={record.id} value={record.id}>{record.title}</option>)}</select></label>{(["impressions", "reach", "clicks", "engagements", "sessions", "conversions"] as const).map((field) => <label key={field}>{field[0].toUpperCase() + field.slice(1)}<input type="number" min="0" value={draft[field]} onChange={(event) => update(field, event.target.value)} /></label>)}<label>Evidence Source<input value={draft.source} onChange={(event) => update("source", event.target.value)} /></label><label className="wide">Notes<textarea rows={3} value={draft.notes} onChange={(event) => update("notes", event.target.value)} /></label></div><ModalActions saving={saving} label="Save Dated Analytics Snapshot" onSave={onSave} /></>; }
function ConfigForm({ draft, setDraft, saving, onSave }: { draft: { senderEmail: string; websiteUrl: string; gaPropertyId: string; linkedInOrganization: string; facebookPage: string }; setDraft: Dispatch<SetStateAction<{ senderEmail: string; websiteUrl: string; gaPropertyId: string; linkedInOrganization: string; facebookPage: string }>>; saving: boolean; onSave: () => void }) { const update = (key: keyof typeof draft, next: string) => setDraft((current) => ({ ...current, [key]: next })); return <><div className="marketing-form-grid"><label>Newsletter Sender<input type="email" value={draft.senderEmail} onChange={(event) => update("senderEmail", event.target.value)} /></label><label>Company Website<input type="url" value={draft.websiteUrl} onChange={(event) => update("websiteUrl", event.target.value)} /></label><label>GA4 Property ID<input value={draft.gaPropertyId} onChange={(event) => update("gaPropertyId", event.target.value)} placeholder="Public property reference only" /></label><label>LinkedIn Organization<input value={draft.linkedInOrganization} onChange={(event) => update("linkedInOrganization", event.target.value)} /></label><label>Facebook Page<input value={draft.facebookPage} onChange={(event) => update("facebookPage", event.target.value)} /></label></div><div className="marketing-publish-boundary"><b>No Secrets Here</b><span>Never paste a password, recovery code, access token, API secret, or private key. Secure authorization belongs in the provider connection workflow.</span></div><ModalActions saving={saving} label="Save Company Marketing Identity" onSave={onSave} /></>; }
function ModalActions({ saving, disabled = false, label, onSave }: { saving: boolean; disabled?: boolean; label: string; onSave: () => void }) { return <footer><button className="primary" disabled={saving || disabled} onClick={onSave}>{saving ? "Saving…" : label}</button></footer>; }
function modalTitle(modal: string) { return ({ campaign: "Campaign Plan", post: "Social Content Studio", newsletter: "Newsletter Studio", analytics: "Analytics Snapshot", survey: "Verified Customer Response", "legacy-survey": "Historical Customer Survey", config: "Company Marketing Setup" } as Record<string, string>)[modal] || "Marketing"; }
function modalSubtitle(modal: string) { return modal === "post" ? "Write the post, attach pictures, choose platforms, and prepare approval." : modal === "newsletter" ? "Create external or employee communication without sending before approval." : modal === "analytics" ? "Record only dated, attributable results—never estimates presented as provider facts." : modal === "survey" ? "Capture ratings, comments, consent, video, and the project team link in one clear record." : modal === "legacy-survey" ? "Create a secure one-off request for completed work that is not in the current project database." : modal === "config" ? "Public identifiers only. Secure credentials stay outside business records." : "Plan the audience, objective, owner, schedule, budget, and result."; }
function connectionLabel(key: string) { return ({ "linkedin-company": "LinkedIn Company", "facebook-company": "Facebook Company", "google-analytics": "Google Analytics 4", "marketing-email": "Marketing Mailbox", "customer-survey-delivery": "Secure Survey Delivery" } as Record<string, string>)[key] || key; }
function connectionPurpose(key: string) { return ({ "linkedin-company": "Publish approved company posts with pictures and retrieve provider-confirmed post performance.", "facebook-company": "Publish approved company posts with pictures and retrieve page and post performance.", "google-analytics": "Read website sessions, traffic sources, engagement, key events, and campaign attribution.", "marketing-email": "Send approved external and internal newsletters from the dedicated company mailbox.", "customer-survey-delivery": "Create secure customer links, verify milestone delivery, receive customer feedback, and preserve separate marketing consent." } as Record<string, string>)[key] || "Controlled marketing connection."; }
